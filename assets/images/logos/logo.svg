<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="100%" height="100%">
  <defs>
    <!-- Mask to slice the '///' out of the letters -->
    <mask id="strikeMask">
      <rect width="200" height="200" fill="white" />
      <!-- Rotate 30 degrees for aggressive slash -->
      <g transform="rotate(30, 100, 100)">
        <!-- Three slash cuts -->
        <rect x="74" y="-50" width="12" height="300" fill="black" />
        <rect x="94" y="-50" width="12" height="300" fill="black" />
        <rect x="114" y="-50" width="12" height="300" fill="black" />
      </g>
    </mask>
  </defs>

  <!-- Using currentColor allows CSS to color the logo! -->
  <g mask="url(#strikeMask)">
    <text x="96" y="154" fill="black" font-family="Impact, Arial Black, system-ui, sans-serif" font-size="140" font-weight="900" letter-spacing="-10" text-anchor="middle">HV</text>
  </g>
</svg>
